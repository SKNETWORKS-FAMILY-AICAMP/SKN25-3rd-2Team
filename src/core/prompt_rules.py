"""한국어 번역 · 요약 프롬프트에 공통으로 적용되는 규칙 모음

역할 2(RAG 응답)와 역할 4(토픽 문서)가 이 규칙을 import해서 재사용한다.
"""

# 한국어 출력 공통 규칙 — system 메시지에 삽입용
KOREAN_OUTPUT_RULES = """\
한국어 출력 규칙:
- 논문에 명시된 내용만 사용한다. 추측, 전망, 과장은 쓰지 않는다.
- abstract를 그대로 옮기지 않는다. 문제 · 접근 · 결과를 재구성해서 전달한다.
- 전문 용어는 논문 원문 표기를 괄호에 병기한다. 예: 주의 메커니즘(attention mechanism)
- 자연스러운 한국어로 쓴다. 영어 어순을 그대로 옮기거나 직역투 표현은 피한다.
  예: "벽시계 시간" → "실제 경과 시간(wall-clock time)", "오버헤드를 감소" → "연산 부담을 줄임"
- 문장은 간결하게 유지한다. 한 문장에 두 가지 이상의 사실을 욱여넣지 않는다.
- "~할 것으로 기대된다", "~에 기여할 수 있다" 같은 미래형 추측 표현은 쓰지 않는다.
- 숫자와 수치는 논문 원문 그대로 표기한다.
"""

# 금지 표현 목록 — 프롬프트 내 명시용
FORBIDDEN_PHRASES = """\
금지 표현:
- "혁신적인", "획기적인", "놀라운" 등 과장 수식어
- "앞으로", "향후", "미래에는" 등 전망성 표현
- "본 논문은 중요한 의미를 가진다" 등 근거 없는 가치 평가
- abstract 첫 문장을 그대로 번역한 문장
- "새로운 접근 방식을 제시한다", "새로운 패러다임을 제시한다" 등 내용 없는 마무리 문장
- "~에 기여한다", "~에 기여할 수 있다" 등 효과를 뭉뚱그리는 표현
- "본 연구에서는", "본 논문에서는" 으로 문장을 시작하는 패턴 (같은 구조 반복 방지)
"""

# 길이 기준 — 역할 2(RAG 응답), 역할 5(UI)가 참조용
LENGTH_RULES = """\
길이 기준:
- 논문 단위 상세 요약: 섹션당 2~4문장. 전체 500자 내외.
- chunk 번역: 원문 길이에 비례. 요약하거나 늘리지 않는다.
- RAG 답변에 쓰이는 한국어 요약: 3~5문장. 근거 chunk 범위를 벗어나지 않는다.
- UI 노출용 짧은 설명(카드 등): 1~2문장, 60자 이내.
"""

# 상세 요약 섹션 구조 — detailed_summary 프롬프트 참조용
SUMMARY_SECTION_STRUCTURE = """\
상세 요약 섹션 구조 (순서 준수):
1. 문제 정의: 이 논문이 풀려는 문제가 무엇인가
2. 접근 방법: 어떤 방식으로 접근했는가 (모델, 알고리즘, 데이터 설계 등)
3. 실험 및 결과: 어떤 실험을 했고 수치 결과는 무엇인가
4. 한계: 논문 스스로 인정하는 한계나 미해결 문제
5. 핵심 가치: 기존 연구와 비교해 이 연구만이 달성한 것이 무엇인가.
   "효율성을 높였다", "새로운 접근 방식을 제시한다", "기여한다" 수준의 요약은 쓰지 않는다.
   반드시 다음 중 하나 이상을 구체적으로 명시한다:
   - 기존 연구가 의존하던 어떤 가정을 이 연구가 버렸는가
   - 어떤 병목을 어떤 구체적 메커니즘으로 제거했는가
   - 기존 방법 대비 구체적으로 무엇이 달라졌는가 (수치 또는 설계 차이)
"""
